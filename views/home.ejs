<% layout('layouts/frontpage') %>

<header class="mb-auto">
    <div>
        <h3 class="float-md-left mb-1">YelpCamp</h3>
        <nav class="nav nav-masthead justify-content-center float-md-right">
            <a href="#" class="nav-link active" aria-current="page">Home</a>

            <% if (!currentUser) {%>
                <a href="/login" class="nav-link">Login</a>
                <a href="/register" class="nav-link">Register</a>
            <% } else { %>
                <a href="/logout" class="nav-link">Logout</a>
            <% } %>
        </nav>
    </div>
</header>

<main class="px-3">
    <h1 class="mb-3">HELLO THERE 👋</h1>
    <p class="lead">Welcome to YelpCamp!<br>Jump right in and explore our many campgrounds.<br>Feel free to
        share some of your own and comment on others!</p>
    <% if (!currentUser) {%>
    <a href="/register" class="btn btn-lg btn-outline-light font-weight-bold mb-0">View
        Campgrounds</a>
    <% } else { %>
        <a href="/campgrounds" class="btn btn-lg btn-outline-light font-weight-bold mb-0">View
            Campgrounds</a>
    <% } %>
</main>

